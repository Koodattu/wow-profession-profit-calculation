# backend

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run start
```

To type-check:

```bash
bun run typecheck
```

The runtime and lockfile are maintained with Bun 1.3.14.
